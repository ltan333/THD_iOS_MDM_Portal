// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/thienel/go-backend-template/internal/ent/apnsconfig"
)

// APNSConfig is the model entity for the APNSConfig schema.
type APNSConfig struct {
	config `json:"-"`
	// ID of the ent.
	ID uint `json:"id,omitempty"`
	// Topic holds the value of the "topic" field.
	Topic string `json:"topic,omitempty"`
	// CertFilePath holds the value of the "cert_file_path" field.
	CertFilePath string `json:"cert_file_path,omitempty"`
	// KeyFilePath holds the value of the "key_file_path" field.
	KeyFilePath string `json:"key_file_path,omitempty"`
	// Expiry holds the value of the "expiry" field.
	Expiry time.Time `json:"expiry,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*APNSConfig) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case apnsconfig.FieldID:
			values[i] = new(sql.NullInt64)
		case apnsconfig.FieldTopic, apnsconfig.FieldCertFilePath, apnsconfig.FieldKeyFilePath:
			values[i] = new(sql.NullString)
		case apnsconfig.FieldExpiry, apnsconfig.FieldCreatedAt, apnsconfig.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the APNSConfig fields.
func (_m *APNSConfig) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case apnsconfig.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = uint(value.Int64)
		case apnsconfig.FieldTopic:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field topic", values[i])
			} else if value.Valid {
				_m.Topic = value.String
			}
		case apnsconfig.FieldCertFilePath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field cert_file_path", values[i])
			} else if value.Valid {
				_m.CertFilePath = value.String
			}
		case apnsconfig.FieldKeyFilePath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key_file_path", values[i])
			} else if value.Valid {
				_m.KeyFilePath = value.String
			}
		case apnsconfig.FieldExpiry:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expiry", values[i])
			} else if value.Valid {
				_m.Expiry = value.Time
			}
		case apnsconfig.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case apnsconfig.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the APNSConfig.
// This includes values selected through modifiers, order, etc.
func (_m *APNSConfig) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this APNSConfig.
// Note that you need to call APNSConfig.Unwrap() before calling this method if this APNSConfig
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *APNSConfig) Update() *APNSConfigUpdateOne {
	return NewAPNSConfigClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the APNSConfig entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *APNSConfig) Unwrap() *APNSConfig {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: APNSConfig is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *APNSConfig) String() string {
	var builder strings.Builder
	builder.WriteString("APNSConfig(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("topic=")
	builder.WriteString(_m.Topic)
	builder.WriteString(", ")
	builder.WriteString("cert_file_path=")
	builder.WriteString(_m.CertFilePath)
	builder.WriteString(", ")
	builder.WriteString("key_file_path=")
	builder.WriteString(_m.KeyFilePath)
	builder.WriteString(", ")
	builder.WriteString("expiry=")
	builder.WriteString(_m.Expiry.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// APNSConfigs is a parsable slice of APNSConfig.
type APNSConfigs []*APNSConfig
