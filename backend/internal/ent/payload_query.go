// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/thienel/go-backend-template/internal/ent/mobileconfig"
	"github.com/thienel/go-backend-template/internal/ent/payload"
	"github.com/thienel/go-backend-template/internal/ent/payloadproperty"
	"github.com/thienel/go-backend-template/internal/ent/predicate"
)

// PayloadQuery is the builder for querying Payload entities.
type PayloadQuery struct {
	config
	ctx              *QueryContext
	order            []payload.OrderOption
	inters           []Interceptor
	predicates       []predicate.Payload
	withMobileConfig *MobileConfigQuery
	withProperties   *PayloadPropertyQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PayloadQuery builder.
func (_q *PayloadQuery) Where(ps ...predicate.Payload) *PayloadQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *PayloadQuery) Limit(limit int) *PayloadQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *PayloadQuery) Offset(offset int) *PayloadQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *PayloadQuery) Unique(unique bool) *PayloadQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *PayloadQuery) Order(o ...payload.OrderOption) *PayloadQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryMobileConfig chains the current query on the "mobile_config" edge.
func (_q *PayloadQuery) QueryMobileConfig() *MobileConfigQuery {
	query := (&MobileConfigClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(payload.Table, payload.FieldID, selector),
			sqlgraph.To(mobileconfig.Table, mobileconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, payload.MobileConfigTable, payload.MobileConfigColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProperties chains the current query on the "properties" edge.
func (_q *PayloadQuery) QueryProperties() *PayloadPropertyQuery {
	query := (&PayloadPropertyClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(payload.Table, payload.FieldID, selector),
			sqlgraph.To(payloadproperty.Table, payloadproperty.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, payload.PropertiesTable, payload.PropertiesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Payload entity from the query.
// Returns a *NotFoundError when no Payload was found.
func (_q *PayloadQuery) First(ctx context.Context) (*Payload, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{payload.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *PayloadQuery) FirstX(ctx context.Context) *Payload {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Payload ID from the query.
// Returns a *NotFoundError when no Payload ID was found.
func (_q *PayloadQuery) FirstID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{payload.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *PayloadQuery) FirstIDX(ctx context.Context) uint {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Payload entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Payload entity is found.
// Returns a *NotFoundError when no Payload entities are found.
func (_q *PayloadQuery) Only(ctx context.Context) (*Payload, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{payload.Label}
	default:
		return nil, &NotSingularError{payload.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *PayloadQuery) OnlyX(ctx context.Context) *Payload {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Payload ID in the query.
// Returns a *NotSingularError when more than one Payload ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *PayloadQuery) OnlyID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{payload.Label}
	default:
		err = &NotSingularError{payload.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *PayloadQuery) OnlyIDX(ctx context.Context) uint {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Payloads.
func (_q *PayloadQuery) All(ctx context.Context) ([]*Payload, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Payload, *PayloadQuery]()
	return withInterceptors[[]*Payload](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *PayloadQuery) AllX(ctx context.Context) []*Payload {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Payload IDs.
func (_q *PayloadQuery) IDs(ctx context.Context) (ids []uint, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(payload.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *PayloadQuery) IDsX(ctx context.Context) []uint {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *PayloadQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*PayloadQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *PayloadQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *PayloadQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *PayloadQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PayloadQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *PayloadQuery) Clone() *PayloadQuery {
	if _q == nil {
		return nil
	}
	return &PayloadQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]payload.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.Payload{}, _q.predicates...),
		withMobileConfig: _q.withMobileConfig.Clone(),
		withProperties:   _q.withProperties.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithMobileConfig tells the query-builder to eager-load the nodes that are connected to
// the "mobile_config" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PayloadQuery) WithMobileConfig(opts ...func(*MobileConfigQuery)) *PayloadQuery {
	query := (&MobileConfigClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMobileConfig = query
	return _q
}

// WithProperties tells the query-builder to eager-load the nodes that are connected to
// the "properties" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PayloadQuery) WithProperties(opts ...func(*PayloadPropertyQuery)) *PayloadQuery {
	query := (&PayloadPropertyClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withProperties = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PayloadDescription string `json:"payload_description,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Payload.Query().
//		GroupBy(payload.FieldPayloadDescription).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *PayloadQuery) GroupBy(field string, fields ...string) *PayloadGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PayloadGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = payload.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PayloadDescription string `json:"payload_description,omitempty"`
//	}
//
//	client.Payload.Query().
//		Select(payload.FieldPayloadDescription).
//		Scan(ctx, &v)
func (_q *PayloadQuery) Select(fields ...string) *PayloadSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &PayloadSelect{PayloadQuery: _q}
	sbuild.label = payload.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PayloadSelect configured with the given aggregations.
func (_q *PayloadQuery) Aggregate(fns ...AggregateFunc) *PayloadSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *PayloadQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !payload.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *PayloadQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Payload, error) {
	var (
		nodes       = []*Payload{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withMobileConfig != nil,
			_q.withProperties != nil,
		}
	)
	if _q.withMobileConfig != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, payload.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Payload).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Payload{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withMobileConfig; query != nil {
		if err := _q.loadMobileConfig(ctx, query, nodes, nil,
			func(n *Payload, e *MobileConfig) { n.Edges.MobileConfig = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withProperties; query != nil {
		if err := _q.loadProperties(ctx, query, nodes,
			func(n *Payload) { n.Edges.Properties = []*PayloadProperty{} },
			func(n *Payload, e *PayloadProperty) { n.Edges.Properties = append(n.Edges.Properties, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *PayloadQuery) loadMobileConfig(ctx context.Context, query *MobileConfigQuery, nodes []*Payload, init func(*Payload), assign func(*Payload, *MobileConfig)) error {
	ids := make([]uint, 0, len(nodes))
	nodeids := make(map[uint][]*Payload)
	for i := range nodes {
		if nodes[i].mobile_config_payloads == nil {
			continue
		}
		fk := *nodes[i].mobile_config_payloads
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(mobileconfig.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "mobile_config_payloads" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *PayloadQuery) loadProperties(ctx context.Context, query *PayloadPropertyQuery, nodes []*Payload, init func(*Payload), assign func(*Payload, *PayloadProperty)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint]*Payload)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.PayloadProperty(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(payload.PropertiesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.payload_properties
		if fk == nil {
			return fmt.Errorf(`foreign-key "payload_properties" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "payload_properties" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *PayloadQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *PayloadQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(payload.Table, payload.Columns, sqlgraph.NewFieldSpec(payload.FieldID, field.TypeUint))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, payload.FieldID)
		for i := range fields {
			if fields[i] != payload.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *PayloadQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(payload.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = payload.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PayloadGroupBy is the group-by builder for Payload entities.
type PayloadGroupBy struct {
	selector
	build *PayloadQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *PayloadGroupBy) Aggregate(fns ...AggregateFunc) *PayloadGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *PayloadGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PayloadQuery, *PayloadGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *PayloadGroupBy) sqlScan(ctx context.Context, root *PayloadQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PayloadSelect is the builder for selecting fields of Payload entities.
type PayloadSelect struct {
	*PayloadQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *PayloadSelect) Aggregate(fns ...AggregateFunc) *PayloadSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *PayloadSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PayloadQuery, *PayloadSelect](ctx, _s.PayloadQuery, _s, _s.inters, v)
}

func (_s *PayloadSelect) sqlScan(ctx context.Context, root *PayloadQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
